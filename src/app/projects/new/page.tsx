import Link from 'next/link';
export default function NewProjectPage() { return <main className="mx-auto max-w-2xl p-8"><h1 className="text-3xl font-semibold">New project</h1><p className="mt-2 text-muted-foreground">Choose a duration and start shaping your video.</p><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-4">{[15,30,60,90,120,180,300,600].map((seconds) => <button className="rounded-lg border bg-card p-4 text-left hover:border-primary" key={seconds}>{seconds < 60 ? `${seconds}s` : `${seconds / 60}m`}</button>)}</div><Link className="mt-8 inline-block text-sm text-primary" href="/dashboard">Back to dashboard</Link></main>; }
